import Log from '../utils/logger.js';

class CardSpr {
  constructor(){
    this.TAG='CardSpr';
    Log.v(this.TAG);
    let stage=new createjs.Container();

    return stage;
  }

}
export default CardSpr;